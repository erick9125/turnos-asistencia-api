APP_NAME="Sistema de Turnos y Asistencia"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=UTC
APP_URL=http://localhost:8000
APP_LOCALE=es
APP_FALLBACK_LOCALE=es
APP_FAKER_LOCALE=es_ES

# Configuración de base de datos
# Soporta: mysql, pgsql, sqlite
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=turnos_asistencia
DB_USERNAME=root
DB_PASSWORD=

# Configuración de Sanctum/API
# Tiempo de expiración de tokens (en minutos)
SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1,localhost:8000
SESSION_DRIVER=database
SESSION_LIFETIME=120

# Configuración de cola
# Opciones: sync, database, redis
# Para desarrollo: sync (ejecuta jobs inmediatamente)
# Para producción: database o redis (requiere worker)
QUEUE_CONNECTION=sync

# Configuración de cache
# Opciones: file, database, redis, memcached
CACHE_DRIVER=file
CACHE_PREFIX=

# Configuración de sesiones
SESSION_DRIVER=database
SESSION_LIFETIME=120

# Configuración de logging
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# API Keys para integraciones externas
# Esta clave se usa para autenticar peticiones desde sistemas externos
# Debe ser una cadena segura y única
EXTERNAL_API_KEY=your-secure-api-key-here-change-in-production

# Configuración de jobs programados
# Intervalo en minutos para asociar marcas con turnos
ASSOCIATE_MARKS_INTERVAL=5
# Hora para procesar ausencias diarias (formato: HH:MM)
DAILY_ABSENCE_TIME=23:59

# Configuración de mail (opcional, para notificaciones futuras)
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Configuración de Redis (opcional, para cache/queue en producción)
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Configuración de broadcasting (opcional)
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync

# Configuración de servicios externos (opcional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

